import { StyledCalculatorContainer } from "../css/StyledComponent";

export const CalculatorContainer = ({ children }) => {
  return <StyledCalculatorContainer>{children}</StyledCalculatorContainer>;
};
